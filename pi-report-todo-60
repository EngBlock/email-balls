PI TODO #60 COMPLETE: Fix emails.ts counter scoping and stale body race

CHANGES MADE:

1. src/store/slices/emails.ts
   - Moved `let _senderFetchSeq = 0` and `let _bodyFetchSeq = 0` from module scope INTO the `createEmailsSlice` function body, mirroring `senders.ts`.
   - Updated the generation-counter comment to accurately describe closure-scoped behavior (counters reset per store creation, never drive rendering).
   - Added `++_bodyFetchSeq` inside `loadEmailsForSender` to invalidate any in-flight body fetch from the previous sender when switching senders.
   - Added `++_bodyFetchSeq` inside `clearEmails` to invalidate any in-flight body fetch when clearing email state.

2. src/store/slices/emails.test.ts
   - Added test: "discards stale body response when the sender is switched"
     - Starts a slow body fetch for Alice
     - Switches to Bob (which bumps `_bodyFetchSeq` and clears body)
     - Resolves Alice's old fetch
     - Asserts body remains `null` (stale response discarded)

VERIFICATION:
- npx vitest run src/store/slices/emails.test.ts: 14 passed (14)
- npx tsc --noEmit: clean (no output)

SCOPE BOUNDARY RESPECTED: Only touched emails.ts and emails.test.ts. No changes to App.tsx, senders.ts, or other slices.
