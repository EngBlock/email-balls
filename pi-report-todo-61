PI TODO #61 COMPLETE: Make signOut cascade to all slices.

CHANGES MADE:
1. src/store/slices/account.ts
   - Updated signOut JSDoc to state it cascades to sibling slices.
   - signOut() now calls get().resetSenders(), get().clearEmails(), get().resetUi() before resetting its own state.

2. src/store/slices/ui.ts
   - Updated resetUi JSDoc to clarify it is automatically invoked by AccountSlice.signOut as part of the sign-out cascade.

3. src/App.tsx
   - Simplified onSignOut to only call signOut().
   - Removed now-unused resetSenders and resetUi action selectors (clearEmails kept as it is used in closeListDrawer).

4. src/store/composed.test.ts
   - Rewrote "signOut + resetSenders + clearEmails + resetUi restores entire store to initial state" → "signOut alone restores entire store to initial state" (only calls signOut, asserts all slices reset).
   - Rewrote "signOut alone does NOT reset senders, emails, or ui state" → "signOut alone resets senders, emails, and ui state" (asserts cascade works).
   - Rewrote "full signOut cascade clears all three error fields" → "signOut alone clears all three error fields" (only calls signOut).

5. src/store/index.test.ts
   - Updated "account signOut resets account slice and leaves ui slice clean" → "account signOut resets account slice and cascades to ui slice", asserting s.error is null after signOut and removed manual resetUi cleanup.

VERIFICATION:
- npx vitest run: 103 tests passed across 9 files.
- npx tsc --noEmit: no errors.
